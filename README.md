# Test
An experimental repo im using for learning the in's and outs of the CI/CD pipeline

## Basic Calculator
This repo now includes a basic browser calculator built with plain HTML, CSS, and JavaScript.

### Run locally
```bash
python -m http.server 8000
```
Then open `http://localhost:8000` in your browser.
